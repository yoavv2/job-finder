# LLM provider API keys — copy this file to `.env` and fill in real values.
# `.env` is gitignored and must NEVER be committed.
# Only the key for your configured llm.provider (see config.yaml) is required.

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
